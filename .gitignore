node_modules/
.next/
out/
build/
.DS_Store
*.log
.vercel
next-env.d.ts
.claude/

# Environment files — never commit real secrets
.env
.env.local
.env.*.local

# env.example files are templates (no real values) — keep them
!**/.env.example
